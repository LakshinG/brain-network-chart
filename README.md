## ADK wrapped agents

Start host agent dev-ui `adk web --port 8021`

Start remote a2a agent `adk api_server --a2a --port 8031 brain_network_chart/remote_a2a`

## CopilotKit[ADK] 

CopilotKit runtime is in `/app/api/copilotkit/route.ts`

`cd copilotkit_runtime`
`npm run dev`